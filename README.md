# White Label Telegram Mini App

Telegram Mini App с white-label интеграцией сторонних сервисов. Поддерживает сайдбар с навигацией и встраивание внешних сервисов без открытия новых Mini Apps.

## Особенности

- ✅ **Сайдбар с навигацией** - переключение между разделами в одном Mini App
- ✅ **White-label интеграция** - встраивание внешних сервисов через iframe
- ✅ **Адаптивный дизайн** - работает на мобильных и десктопах
- ✅ **Адаптация под тему Telegram** - автоматическое переключение светлой/темной темы
- ✅ **Навигация без перезагрузки** - всё происходит в одном окне

## Структура

- **Вишлист** - главная страница приложения
- **ИИ помощник в подарках** - встроенный сервис (quetlink-five.vercel.app)

## ⚠️ Важно: Модификация встроенного сервиса

**Встроенный сервис должен быть модифицирован для работы в iframe!**

Когда Mini App встраивается в iframe, он не получает параметры Telegram напрямую. Родительское приложение передает параметры через `postMessage` API, но встроенный сервис должен уметь их принимать.

### Быстрое решение:

1. Добавьте файл `iframe-telegram-bridge.js` в ваш встроенный сервис
2. Подключите его перед инициализацией вашего приложения:
   ```html
   <script src="/iframe-telegram-bridge.js"></script>
   ```

Подробные инструкции смотрите в файле [INTEGRATION_GUIDE.md](./INTEGRATION_GUIDE.md)

## Локальная разработка

```bash
npm install
npm run dev
```

## Деплой на Vercel

1. Установите Vercel CLI (если еще не установлен):
```bash
npm i -g vercel
```

2. Задеплойте проект:
```bash
vercel
```

Или используйте веб-интерфейс Vercel:
- Зайдите на [vercel.com](https://vercel.com)
- Импортируйте репозиторий
- Vercel автоматически определит настройки

## Настройка бота в Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot` и следуйте инструкциям
3. Отправьте команду `/newapp` и выберите вашего бота
4. Укажите URL вашего Mini App (например: `https://your-app.vercel.app`)
5. Отправьте команду `/setmenubutton` и укажите текст кнопки и URL

## Настройка white-label интеграции

Для добавления нового сервиса в сайдбар:

1. Добавьте новый пункт меню в HTML:
```html
<a href="#" class="menu-item" data-page="your-service" onclick="navigateTo('your-service', event)">
    <span class="menu-item-icon">🔧</span>
    <span class="menu-item-text">Ваш сервис</span>
</a>
```

2. Добавьте контейнер для страницы:
```html
<div class="iframe-container" id="your-service-page" style="display: none;">
    <iframe 
        class="embedded-service" 
        src="https://your-service.com"
        allow="camera; microphone; geolocation"
        sandbox="allow-same-origin allow-scripts allow-forms allow-popups allow-modals"
    ></iframe>
</div>
```

3. Добавьте обработку в функцию `navigateTo()`:
```javascript
else if (page === 'your-service') {
    document.getElementById('your-service-page').style.display = 'block';
    document.getElementById('page-title').textContent = 'Ваш сервис';
    currentPage = 'your-service';
    tg.MainButton.hide();
}
```

## Тестирование

После деплоя откройте бота в Telegram и нажмите на кнопку меню или используйте команду для запуска Mini App.

## Технические детали

- Использует Telegram Web App API версии 59
- Поддерживает все основные функции Telegram Mini Apps
- Iframe изолирован с помощью sandbox атрибутов для безопасности
- Адаптивная верстка с поддержкой мобильных устройств
